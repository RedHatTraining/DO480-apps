kind: Route
metadata:
  labels:
    app: todonodejs
    name: frontend
  name: frontend
  namespace: mysql
spec:
  host: todo.apps.ocp4-mng.example.com
  port:
    targetPort: 8080
  to:
    kind: Service
    name: frontend
    weight: 100
  wildcardPolicy: None
